<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>golfcart_sensor_kit_launch</name>
  <version>0.1.0</version>
  <description>The golfcart_sensor_kit_launch package</description>
  <maintainer email="ryohsuke.mitsudome@tier4.jp">Ryohsuke Mitsudome</maintainer>
  <maintainer email="yukihiro.saito@tier4.jp">Yukihiro Saito</maintainer>
  <license>Apache License 2.0</license>

  <buildtool_depend>ament_cmake_auto</buildtool_depend>

  <exec_depend>autoware_gnss_poser</exec_depend>
  <exec_depend>autoware_pointcloud_preprocessor</exec_depend>
  <exec_depend>topic_tools</exec_depend>
  <!-- Both come from the ublox_f9p_ws submodule under
       src/sensor_component/external, built with the workspace: ublox_gps is
       a fork that takes RTCM as mavros_msgs/RTCM, which is what that
       workspace's ntrip_client publishes. rosdep must not install
       ros-humble-ublox-gps for this key, and does not while the package is in
       the source tree (the ros-deps setup step passes ignore-src). -->
  <exec_depend>ublox_gps</exec_depend>
  <exec_depend>ntrip_client</exec_depend>
  <exec_depend>nmea_navsat_driver</exec_depend>
  <exec_depend>seyond</exec_depend>
  <exec_depend>xsens_mti_can_ros_driver</exec_depend>

  <!-- gmslcam (a workspace package, src/sensor_component/external/gmslcam)
       bridges GStreamer into ROS and declares only gstreamer1.0-tools, not
       the plugin packages a real camera pipeline loads at runtime. Declaring
       them here is what lets rosdep install the camera stack, so setup needs
       no step of its own for it. -->
  <exec_depend>gmslcam</exec_depend>
  <exec_depend>gstreamer1.0-plugins-base</exec_depend>
  <exec_depend>gstreamer1.0-plugins-good</exec_depend>
  <exec_depend>gstreamer1.0-plugins-bad</exec_depend>
  <exec_depend>gstreamer1.0-plugins-ugly</exec_depend>
  <exec_depend>gstreamer1.0-tools</exec_depend>

  <test_depend>ament_cmake_pytest</test_depend>
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>autoware_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
